# Read a CCE specification from YAML

Read a CCE specification from YAML

## Usage

``` r
read_cce_spec(path)
```

## Arguments

- path:

  YAML file created by [`write_cce_spec()`](write_cce_spec.md).

## Value

A `cce_spec` object.
