# Write CCE result files to disk

Write CCE result files to disk

## Usage

``` r
write_cce_results(x, path = "outputs")
```

## Arguments

- x:

  A `cce_vs_result` or `cce_soc_result`.

- path:

  Output directory.

## Value

Invisibly returns `path`.
