# Generate bundled synthetic example data

Generate bundled synthetic example data

## Usage

``` r
cce_demo_data(n = 250L, seed = 42L)
```

## Arguments

- n:

  Number of synthetic patients.

- seed:

  Random seed.

## Value

A list with normalized source tables and an analysis-ready dataset.
