# Write a CCE specification to YAML

Write a CCE specification to YAML

## Usage

``` r
write_cce_spec(spec, path)
```

## Arguments

- spec:

  A `cce_spec` object.

- path:

  File path ending in `.yml` or `.yaml`.

## Value

Invisibly returns `path`.
